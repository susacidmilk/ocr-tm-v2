# 更新日志 / Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 首个公开版本

首个开源版本，Windows 桌面 OCR 游戏 / 视觉小说翻译器。

### 新增

- **屏幕 OCR**：鼠标框选识别区域；`ocr-worker.ps1` 常驻进程调用 Windows 自带 OCR（英文优先）；typewriter 稳定帧判定避免把半句当整句；乱码自动停止。
- **实时逐句翻译**：识别到即翻译，结果进历史与翻译记忆库。
- **翻译源可切换**：☁️ DeepSeek（远程，OpenAI 兼容）与 🔌 本地 LM Studio，两源各自独立保存 API Key / Base / 模型。
- **批量录制 → 整段翻译**：挂机收集对白成清单；整段编号提交；按目标每段 ~70 句自动均分；并发段数可调（默认 3）；可传 `reasoning_effort`（默认 `low`）。
- **本地翻译记忆库（TM）**：按游戏隔离；精确命中秒显不重复调 API；相似句以"疑似命中"形式交由用户确认。
- **历史**：同句只留一行（记录最早/最新时间与出现次数）；单句重翻、删除；两种显示方式（两行完整 / 单行紧凑）。
- **术语表**：按游戏维护 `原文 → 译文 #备注`，翻译时作为约束带入。
- **置顶悬浮层**：透明无边框、点击穿透可解锁拖动；译文/原文字号与宽度可调；自动避开识别区域；关闭主窗口即退出。
- **识别区域常驻提示框**：可开关的浅色虚线框，画在采样区外侧，不影响 OCR 截图。
- **质量与健壮性**：整段翻译长度硬校验 + 二分/单句兜底；DeepSeek 偶发"回显原文"自动识别并重试；翻译看门狗超时跳过；单句重翻与整段翻译强制走 DeepSeek。
- **本地提示词**：LM Studio 使用视觉小说模板（`[History]` / `[Glossary]` / `[Input]`）；被 DeepSeek 修正过的历史行标 `[corrected]` 作为权威参考。
- **导出**：翻译记忆库导出 JSON / TXT。
- **工程**：模块化分层（main / preload / renderer）、离线自测 72 项、GitHub Actions CI、双击即用的 `启动.cmd`。

### 已知限制

- 仅支持 Windows；仅主显示器、单识别框。
- OCR 目前依赖系统英文语言包（多语种见 [路线图](ROADMAP.md)）。
- 界面目前仅中文。
- 悬浮层不能实时覆盖在原文字幕上（会把自身像素截进 OCR）。

[1.0.0]: https://github.com/susacidmilk/ocr-tm-v2/releases/tag/v1.0.0
